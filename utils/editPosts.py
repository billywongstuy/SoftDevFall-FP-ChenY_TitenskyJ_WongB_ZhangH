def addPost(user, title, content):
	db=sqlite3.connect('data/info.db')
    c=db.cursor()
    q="select "
    postID=
    q="INSERT INTO posts VALUES (\""+user+'\", \"'+myHashObj.hexdigest()+'\",' + "\"" + prefLang+"\",\""+ nativeLang + "\")"
